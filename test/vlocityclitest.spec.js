'use strict'
var fs = require('fs-extra');
var path = require('path');

const expect = require('chai').expect;
const _vlocitycli = require('../lib/vlocitycli');

describe('VlocityCLI', async () => {
    
    it('Run Test Job', async () => { 
       /* var vlocitycli = new _vlocitycli();
        let result = await vlocitycli.runCommands([ 'runTestJob' ], {
            "sfdx.username": "VB_TEST_ORG", 
            verbose: true,
            argv: {}
        });

        expect(result.exitCode).to.be.eq(0);*/
    }).timeout(0);
});